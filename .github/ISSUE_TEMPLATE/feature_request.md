---
name: Feature request
about: Propose a change to the compiler, IR, analyzer, or docs.
title: "feat: <short description>"
labels: enhancement
---

> Before filing: if this is a **new `ir_kind`** (e.g. `pptx-canvas`, `data-table`), please file a **Hero case proposal** instead — new kinds need a real triggering skill to enter the project. See `DESIGN-v0.2.md` §11 (spike-gating) and `TODO.md`.

## What's the problem

<!-- Concrete situation that motivates this. Avoid abstract "it would be nice if". -->

## What you're proposing

<!-- The smallest change that fixes the problem. Touch the fewest places possible. -->

## Why this fits skill2web

<!-- Cross-check against the invariants the project holds: -->

- [ ] The compiled artifact stays **single-file HTML, no agent runtime**.
- [ ] The build-time tooling stays a Claude skill, not a service.
- [ ] No new `ir_kind` without a real triggering skill (or this is not a new kind).

## Alternatives considered

<!-- What else could solve the same problem? Why is your proposal better? -->

## Anything else

<!-- Mockups, IR snippets, links to similar implementations elsewhere. -->
